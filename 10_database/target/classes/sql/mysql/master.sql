USE MT_POC;

CREATE TABLE MT_MASTER (
    ID BIGINT PRIMARY KEY,

    ENTERO INT,
    LARGO  BIGINT,
    MONEDA DECIMAL(10,2),
    FECHA  DATE,
    TMS    TIMESTAMP,
    FCHAR  CHAR(10),
    VCHAR  VARCHAR(255),
    LOGICO BOOLEAN
);
