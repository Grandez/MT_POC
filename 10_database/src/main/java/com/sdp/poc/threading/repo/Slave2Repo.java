package com.sdp.poc.threading.repo;

import com.sdp.poc.threading.entity.Slave2;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Slave2Repo  extends JpaRepository<Slave2, Long> {
}
