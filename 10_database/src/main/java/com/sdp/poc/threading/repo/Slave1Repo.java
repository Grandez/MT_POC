package com.sdp.poc.threading.repo;

import com.sdp.poc.threading.entity.Slave1;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Slave1Repo  extends JpaRepository<Slave1, Long> {
}
